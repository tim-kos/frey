#{{ ansible_managed }}

*** Probes ***

+ FPing

binary = /usr/bin/fping

+ Curl

# probe-specific variables
binary = /usr/bin/curl
step = 60

# a default for this target-specific variable
urlformat = http://%host%/
