#{{ ansible_managed }}

*** Targets ***

probe = FPing

menu = Top
title = Network Latency Grapher
remark = Welcome to the SmokePing website of {{smk_company}}. \
         Here you will learn all about the latency of our network.

+ Network

menu = Network
title = Network

{% for target_name, target in smk_targets.iteritems() %}

++ {{target_name}}

{% if target.probe |default() %}
probe = {{target.probe}}
{% endif %}
{% if target.urlformat |default() %}
urlformat = {{target.urlformat}}
{% endif %}
title = {{target.title |default(target_name)}}
host = {{target.host}}
alerts = {{target.alerts |default("someloss")}}
{% endfor %}
